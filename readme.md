Концепция сплиттинга для ИЕ

Через хтмл теги для нужной версии добавляем префикс на body (body.ie9), а на уровне стилей просто переопределяем базовые стили теми, что лежат под префиксом.

```css
.app {}

.ie9 .app {}
```
